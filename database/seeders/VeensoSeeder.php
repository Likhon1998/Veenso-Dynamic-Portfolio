<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * Demo website content is installed via:
 * php artisan veenso:install-demo
 *
 * Use --fresh to wipe demo content tables before importing.
 * Admin users remain in AdminUserSeeder only.
 */
class VeensoSeeder extends Seeder
{
    public function run(): void
    {
        // Demo content is installed via `php artisan veenso:install-demo`.
    }
}
